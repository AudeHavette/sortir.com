{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Profil {% endblock %}

{% block body %}
    <h1>Mon profil</h1>
    <div class="container">
        {{ form_start(formProfil) }}
        {{ form_widget(formProfil) }}
        <button type="submit">
            {{ btnLabel|default('Modifier') }}
        </button>
        {{ form_end(formProfil) }}
    </div>


{% endblock %}
